/** Working inbox for support, privacy, and legal inquiries. */
export const SUPPORT_EMAIL = "kynoggames@gmail.com";

export const SUPPORT_MAILTO = `mailto:${SUPPORT_EMAIL}` as const;

/** User-facing expectation for reply time (Contact + legal pages). */
export const SUPPORT_RESPONSE_EXPECTATION =
  "We will get back to you — we typically respond within 24–48 hours.";
